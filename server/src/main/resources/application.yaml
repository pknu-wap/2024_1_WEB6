jwt:
  secret: Wt7Z0OTntmZTsnbTtjIXtlZzqta3snYTrhIjrqLjshLjqs4TroZzrgpjslYTqsIDsnpDtm4zrpa3tlZzqsJzrsJzsnpDrpbzrp4zrk6TslrTqsIDsnpA=

#spring:
#  data:
#    redis:
#      host: localhost
#      port: 6379

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/netview"
    username: "root"
    password: ""
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: ture
        show_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

  # 소셜로그인 설정
  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: 7df825c06a3162cabc499582d021d248
            client-secret: GTOMlmMBY5aG1bi0DZPDSuZNeAc3J14H
            client-authentication-method: client_secret_post
            authorization-grant-type: authorization_code
            scope: # https://developers.kakao.com/docs/latest/ko/kakaologin/common#user-info
              - profile_nickname
              - profile_image
              - account_email
            redirect-uri: "{baseUrl}/{action}/oauth2/code/{registrationId}"
            #            redirect-uri: http://localhost:8080/login/oauth2/code/kakao
            client-name: Kakao

        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-info-authentication-method: header
            user-name-attribute: id # Kakao 응답 값 id, connected_at, properties, kakao_account 중 id 지정
#  profiles: include=oauth
server:
  servlet:
    session:
      timeout: 90m
    encoding:
      charset: UTF-8
      enabled: true
      force: true
  port: 8080
